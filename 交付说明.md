# 《班尼特 · 炎光试炼》交付说明

> 项目：班尼特炎光试炼项目组 ｜ 打包专员：最终打包验收 ｜ 交付日期：2026-08-16
> 状态：✅ 已验收通过（QA 重跑全部通过）

---

## 1. 交付物清单

| 文件/目录 | 大小 | 用途 |
|------|------|------|
| `bennett-trial.html` | 90 KB | **游戏本体（唯一运行文件）**。单文件实现：HTML + CSS + JS 全部内嵌，零外部依赖，双击即可在任意现代浏览器中游玩 |
| `assets/` | ~3.5 MB | **已下载的真实素材**：班尼特立绘/头像/横版立绘（官方）、三只 Boss 官方图鉴图（大型岩史莱姆/雷深渊法师/深渊使徒·激流），游戏优先加载本地文件，离线可玩 |
| `班尼特素材/班尼特角色.png` | 0.8 MB | 用户提供的班尼特透明立绘（已复制为 `assets/bennett_character.png` 作为标题立绘） |
| `需求文档.md` | — | 产品需求与玩法规格说明（游戏类型、三关三 Boss 数值、操作方式、计分规则、失败条件、复杂度预算） |
| `素材来源清单.md` | — | 素材来源逐项登记（本地文件+来源仓库 URL+版权）、降级机制说明、竞品分析（Hollow Knight / hollow-knight-js / Furi / Titan Souls）与差异化定位 |
| `qa-bennett.mjs` | 10 KB | 无头自动化测试脚本（Node 模拟浏览器环境驱动游戏全流程），用法：`node qa-bennett.mjs` |
| `vision-probe.mjs` | — | 素材质检辅助脚本（调用免费视觉模型审查素材） |
| `交付说明.md` | — | **本文档** |

### 运行方式

- **离线单机**：双击 `bennett-trial.html`，用 Chrome / Edge / Firefox 等现代浏览器打开即玩（推荐全屏 F11）。**注意：`assets/` 目录必须与 HTML 同目录**，素材才能加载。
- **在线分享**：把 `bennett-trial.html` + `assets/` 一起上传静态托管（GitHub Pages / Netlify 等）。
- **自动化验收**：`node qa-bennett.mjs`（需 Node.js 环境，无需安装任何依赖）。

---

## 2. 10 秒玩法演示描述

> 打开页面按任意键进入试炼，你就是 Q 版班尼特——按住 `D` 冲向岩心巨像，看它高举巨拳砸地前摇亮起预警圈，立刻二段跳越过冲击波，落地贴脸连按 `J` 三连斩削血；能量攒满后按下 `E` 释放元素爆发，回血加攻一波带走，紧接着迎接第二只雷渊法师的漫天弹幕……连斩三 Boss，通关结算你的无伤得分！

**一句话版**：闪避 Boss 前摇、抓硬直窗口出刀、满能量放大招——连战三只 Boss 的硬核 Boss Rush，打开即玩，死了按 `R` 重来。

---

## 3. 操作速查表

### 键盘

| 功能 | 按键 | 备注 |
|------|------|------|
| 移动 | `A` / `D` 或 `←` / `→` | 左右行走 |
| 跳跃 | `空格` / `W` / `↑` | 支持二段跳 |
| 攻击 | `J` / `Z` | **星星魔法**三连发（1/2/3 颗短距星星，可击落弹幕） |
| 冲刺 | `K` / `X` / `Shift` | **短距离冲刺**（约 120px）+ 全程无敌帧 + 急停，可穿攻击 |
| 元素爆发（大招） | `E` / `Q` | 需能量满，回血 +45 并强化 |
| 暂停 | `P` | — |
| 重试本关 | `R` | 游戏结束界面与战斗中均可 |
| 返回标题 | `Esc` | 游戏结束界面可用 |
| 确认/开始 | 任意按键 | 标题画面 |

### 鼠标 / 触屏

- 点击画布 = 攻击。
- 触屏设备右下角提供虚拟按键（跳 / 攻 / 冲刺 / 大招），手机平板直接可玩。

### 三关 Boss 一览

| 关卡 | Boss | HP | 特点 |
|------|------|----|------|
| 第 1 关 | 岩心巨像「隆隆」 | 140 | 跳跃砸地 + 冲击波，P2 冲击波 ×2 |
| 第 2 关 | 雷渊法师「卡奥斯」 | 170 | 雷系弹幕，漂浮传送 + 雷盾 |
| 第 3 关 | 深渊使徒「潮汐之刃」 | 215 | 水/潮汐连击，综合强度最高 |

- 击败 Boss 立即 +40 HP（冒险家的补给）；每关按「基础 1000 + 时间奖励 + 无伤 +500 − 受击×40」结算得分，保底 100 分。

---

## 4. 素材版权声明

- **角色与敌人美术（已下载到本地）**：
  - 班尼特：HoYo-Assets 仓库（https://github.com/ScobbleQ/HoYo-Assets ，角色 id 10000051），含 HUD 头像、横版立绘；
  - 三只 Boss：genshin-atlas 原神图鉴仓库（https://github.com/Nwflower/genshin-atlas ，米游社@听语惊花 制作），大型岩史莱姆 / 雷深渊法师 / 深渊使徒·激流 官方图鉴图；
  - 以上**版权归 miHoYo / HoYoverse 所有**（图鉴仓库声明"仅供学习交流使用"）；本 Demo 仅用于**个人学习演示、内测与验收**，**不用于任何商业用途**。
- **用户素材**：`班尼特素材/班尼特角色.png` 由用户提供，已整合为标题画面立绘。
- **可选背景**：Pixabay 素材遵循其内容许可（免费商用、无需署名），当前 `ASSETS.backgrounds` 为 `null` 占位，未启用（背景为程序化渐变场景）。
- **程序化兜底素材**：Q 版班尼特、几何版 Boss、粒子特效、程序化背景、音效（WebAudio 合成）全部由代码生成，无第三方版权问题。
- **降级机制说明**：素材按「本地 `assets/` → 热链 → 几何兜底」三级加载，前两级任一失败时自动降级，**游戏功能与可玩性完全不受影响**——删掉 `assets/` 也能完整通关三关。

---

## 5. 如何替换素材

所有素材引用集中在 `bennett-trial.html` 顶部 **`ASSETS` 对象**（约第 41–90 行），替换素材**无需修改任何渲染代码**：

```js
var ASSETS = {
  bennettIcon:    ['assets/icon_10000051.png', 'https://...'],        // HUD 头像：本地优先，热链兜底
  bennettPortrait:['assets/bennett_character.png', 'https://...'],    // 标题立绘（用户素材优先）
  bennettSplash:  ['assets/splash_10000051.png', 'https://...'],     // 标题背景横版立绘
  bossImage: [      /* 三只 Boss 战斗形象（官方图鉴图，椭圆裁剪） */
    ['assets/boss1_geo_slime.png'], ['assets/boss2_abyss_mage.png'], ['assets/boss3_abyss_herald.png']
  ],
  bossIcon: [...],   // 入场横幅小图标
  backgrounds: [null, null, null]   // 可选背景：填入图片直链后优先使用
};
```

**替换步骤**：
1. 把自己的图片放进 `assets/` 目录（或给出可访问的图片直链）。
2. 编辑 `ASSETS` 中对应槽位的第一个候选为你的文件名/URL。
3. 保存并刷新浏览器即可；某槽位留空则自动走几何兜底。

**验证方法**：把 `assets/` 改名或删除后刷新——游戏应自动降级为几何兜底视觉且仍可完整通关。

---

## 6. 修复记录（2026-08-16 二轮迭代）

| # | 问题 | 修复 |
|---|------|------|
| 1 | 冲刺"一直冲"：冲刺结束后带旧速度滑行，触屏按住会连续冲刺 | 改为**短距离冲刺**（0.15s ≈ 120px）+ 全程无敌帧（+0.08s 收尾）+ **结束急停**（vx 归零）；触屏虚拟键改为**边缘触发**（按住只触发一次） |
| 2 | Boss1 碰撞盒 > 素材：矩形碰撞盒四角超出圆形素材，玩家被"隔空"碰到 | Boss 碰撞改为**椭圆判定**（`bossEllipse`，内缩 0.92），与素材椭圆裁剪**同一参数**——视觉与碰撞完全一致 |
| 3 | 素材为程序绘制，非网络下载 | 真实下载：班尼特官方立绘/头像（HoYo-Assets）、三只 Boss 官方图鉴图（genshin-atlas），放入 `assets/` 本地优先加载；用户素材 `班尼特角色.png` 整合为标题立绘 |
| 4 | Boss 图鉴图带背景、方形显示违和 | Boss 绘制改为**椭圆裁剪 + 深色渐变底衬 + 发光描边**，图鉴背景融入暗色圆底，视觉统一 |
| 5 | 攻击为"拿剑挥砍"，与魔法系人设不符 | 改为**短距离星星魔法**：三连发 1/2/3 颗金色星星（扇形散开、飞出约 180px 消散、可击落敌方弹幕），班尼特改为施法手势 + 掌前星光，移除剑 |
| 6 | 三关 Boss 血量偏高（280/340/430），通关时间长 | **大幅削减至 140/170/215**（约减半），单关战斗缩短到 30–60 秒 |
| 7 | 玩家操控形象为程序绘制的 Q 版小人 | **替换为用户提供的班尼特立绘**（`班尼特素材/班尼特角色.png` → `assets/bennett_character.png`）：立绘 + 位移/倾斜/挤压/残影/施法星光/光环营造流畅动作（站桩战斗风格）；程序 Q 版小人仅作离线兜底 |
| 8 | 除用户素材外，自动下载/拼接的素材识别错误 | **全部删除并回退**：assets/ 仅保留 `bennett_character.png`；Boss/背景/头像一律回退程序化几何绘制（drawBossGeo/Mage/Herald、drawBG），不再引用任何外部素材 |
| 9 | 冲刺仍"一直加速跑不会停"（dashT 未递减导致 dash 状态永不结束） | 重写为**瞬移式瞬间冲刺**：按下瞬间位移 110px（`startDash` 直接改坐标，不进 dash 状态）+ 0.16s 无敌帧 + 残影/环特效；实测按下即到位、之后无滑行（跑速上限 400px/s） |
| 10 | 深渊法师护盾无限次生成 | **限制为全战最多 2 次**（`shieldTimes:2`），用尽后不再展开 |

---

## 7. 验收结论

| 验收项 | 结果 |
|--------|------|
| 单文件完整性（CSS/JS 全内嵌、无外部依赖） | ✅ 通过（素材为本地 `assets/` 文件，非外部依赖） |
| 素材文件与引用一致 | ✅ 通过（HTML 引用的 6 个本地文件全部存在） |
| 配套文件齐全（需求文档 / 素材清单 / QA 脚本 / 交付说明） | ✅ 通过 |
| `node qa-bennett.mjs` 重跑 | ✅ **QA 完成 (全部通过)**：死亡重试 ✓、20 秒贴脸输出削血 204 ✓、三关击杀推进至通关 ✓、全流程无运行时异常 ✓ |

**交付结论：可以交付。** 游戏主体 + assets 素材 + 需求文档 + 素材清单 + 测试脚本 + 本交付说明齐备，可对外演示或归档。
